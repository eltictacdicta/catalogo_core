# Tasks: Tarifa selector + htmx 4 / Alpine CSP for opcionales

- **Change**: `opcionales-tarifa-selector-htmx4`
- **SDD owner**: `plugins/catalogo_core/openspec/` (`ownership: plugin-local`; core `openspec/` untouched)
- **Spec**: `specs/opcionales-tarifa-selector/spec.md` (OTS-01..OTS-10)
- **Design**: `design.md` (WU-1..WU-5 sequence, AD-1..AD-8, §5 interfaces, §6 test split)
- **Runner**: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`
- **Baseline**: 455 tests / 1381 assertions OK (must stay green, plus the new tests)
- **Repo boundary**: `plugins/catalogo_core/` is an independent git repository (`origin` `catalogo_core`, branch `main`). The parent repo ignores plugin paths (`.gitignore:38 /plugins/*`), so every chain slice lands in the plugin repo and the parent repo is never touched.
- **Strict TDD**: every phase authors/runs RED before implementing GREEN.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~850–1050 authored diff (WU-1 ~340, WU-2 ~60, WU-3 ~330, WU-4 ~45, WU-5 ~75; new tests are ~150 lines in `TarifOpcionalEditTarifaSelectorTest.php` + ~170 in `TarifOpcionalesHtmxContractTest.php`). All under `plugins/catalogo_core/`; app-code net is small (controller ~+110/−40; three views ~+270/−220). |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 5 stacked PRs, one per WU (WU-1 selector + scoped panel → WU-2 htmx boot + selector swap → WU-3 Alpine CSP + POST/CSRF migration → WU-4 precios selector → WU-5 list filters); each slice stays ≤400 changed lines and each ends with the full plugin suite green. |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No — delivery strategy (`auto-chain`) and chain strategy (`stacked-to-main`) are pre-decided; no user fork remains.
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | PR | Focused test | Runtime harness | Rollback boundary |
|------|------|----|--------------|-----------------|-------------------|
| WU-1 | `tarif_opcional_edit` exposes a validated `tarifa_seleccionada`, scopes the "Precios por tarifa" panel, keeps the compact all-tarifas overview, and preserves every locked string/contract (OTS-01..OTS-03, OTS-05, OTS-09). | PR 1 (stacked, targets `main`) | `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifOpcionalEditTarifaSelectorTest.php` + `tests/TarifOpcionalesControllerMasterStateTest.php` | dev-DB smoke (deferred to verify): `page=tarif_opcional_edit&id=N&codtarifa=T2` renders the selector + tarifa-scoped panel; unknown `codtarifa` falls back. | Revert WU-1 commit (controller resolver + view panel + new test). |
| WU-2 | The selector drives an htmx 4 full-page body swap (`hx-get`/`hx-trigger="change"`/`hx-target`+`hx-select="body"`/`hx-swap`/`hx-push-url`), with no full-page `onchange` submit (OTS-04). | PR 2 (stacked on WU-1) | `tests/TarifOpcionalesHtmxContractTest.php::test_edit_view_boots_htmx_and_alpine_macros` + `tests/TarifOpcionalEditTarifaSelectorTest.php::test_edit_view_selector_has_no_full_page_submit` | Browser smoke (deferred to verify): selector change swaps `<body>` and pushes the URL. | Revert WU-2 commit (view htmx attrs + boot + contract assertions). |
| WU-3 | Confirm/delete flows and article search migrate to Alpine CSP (`Alpine.data()` via nonce'd script + `alpine:init` guard), GET mutations move to `hx-post` + `requireCsrf()`, idempotent re-init on `htmx:after:swap`, colon event names only (OTS-06, OTS-07). | PR 3 (stacked on WU-2) | `tests/TarifOpcionalesHtmxContractTest.php` (Alpine/colon/POST/idempotency methods) + `tests/TarifOpcionalesControllerMasterStateTest.php` | Browser smoke (deferred to verify): confirm modal + article search work, no console CSP/bootbox errors, no duplicate handlers after swap. | Revert WU-3 commit (controller POST guards + Alpine view/script + tests). |
| WU-4 | `tarif_opcional_precios` selector migrates to `hx-get` + URL push; save form stays a normal POST with locked strings (OTS-08). | PR 4 (stacked on WU-3) | `tests/TarifOpcionalesHtmxContractTest.php::test_precios_selector_uses_hx_get_with_url_push` + `::test_list_and_precios_views_preserve_locked_names_csrf_and_toggles` | Browser smoke (deferred to verify): precios selector swaps the page and pushes the URL; save still persists. | Revert WU-4 commit (precios view selector attrs + test assertions). |
| WU-5 | `tarif_opcionales` filters migrate to `hx-get` + URL push while the locked POST toggles/names/CSRF stay byte-identical (OTS-08). | PR 5 (stacked on WU-4) | `tests/TarifOpcionalesHtmxContractTest.php::test_list_filters_use_hx_get_with_url_push` + `::test_list_and_precios_views_preserve_locked_names_csrf_and_toggles` | Browser smoke (deferred to verify): list filters swap and push the URL; row toggles still POST with CSRF. | Revert WU-5 commit (list view filters + test assertions). |

Chain mechanics: branch `change/opcionales-tarifa-selector-htmx4-wu1` off `main`, then `...-wu2` off `...-wu1`, and so on (`stacked-to-main`); each PR targets the previous branch and the final PR targets `main`. `auto-chain` opens the next slice PR as soon as the previous one is pushed; apply never pushes or opens PRs by itself.

## Phase 1 — WU-1: selector resolution + scoped panel (OTS-01..OTS-03, OTS-05, OTS-09)

Target files: `plugins/catalogo_core/controller/tarif_opcional_edit.php`, `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`, `plugins/catalogo_core/tests/TarifOpcionalEditTarifaSelectorTest.php` (new), `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` (new, controller methods only).

- [x] 1.1 **RED** — Create `plugins/catalogo_core/tests/TarifOpcionalEditTarifaSelectorTest.php` (namespace `Tests\CatalogoCore`; `require_once` the controller chain like `TarifCatalogoHtmxContractTest::setUpBeforeClass()`) with the DB-free behaviour methods that Reflection-invoke `resolver_tarifa_seleccionada()` on an anonymous `extends \tarif_opcional_edit` subclass that skips the constructor: `test_selector_resolution_prefers_requested_active_tarifa`, `test_unknown_codtarifa_falls_back_to_default`, `test_absent_codtarifa_falls_back_to_default`, `test_unknown_codtarifa_without_default_falls_back_to_first_active`, `test_no_active_tarifas_yields_false_selection`. Each sets `$codtarifa` / `$tarifas` / `$tarifa_defecto`, invokes the private method via Reflection, and asserts `$tarifa_seleccionada->codtarifa` plus normalized `$codtarifa`. Design §6 (rows 1–2). Run: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifOpcionalEditTarifaSelectorTest.php` → RED (method/property missing). ~150 lines.
- [x] 1.2 **RED** — Add the source-contract methods for the selector and scoped panel to the same `TarifOpcionalEditTarifaSelectorTest.php`: `test_edit_view_exposes_selector_listing_tarifas_with_default_marked`, `test_edit_view_selector_has_no_full_page_submit` (OTS-01, OTS-04), `test_edit_view_scoped_panel_preserves_locked_save_contract`, `test_edit_view_retains_all_tarifas_overview` (OTS-02, OTS-05). Read `plugins/catalogo_core/View/tarif_opcional_edit.html.twig` with `file_get_contents` and assert the selector/list `tarifas`, the scoped `guardar_precio_tarifa` + `{{ csrf_field() }}` + `tarif.toggle_button_group(`, an all-tarifas loop over `fsc.precios_tarifas`, and the absence of `onchange="this.form.submit()"` on the tarifa selector. Design §6 (rows 3–4). Run the file → RED. ~40 lines.
- [x] 1.3 **RED** — Create `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` (namespace `Tests\CatalogoCore`; source-contract style reading real template/controller files) with the controller methods `test_edit_controller_rejects_unknown_tarifa_and_exposes_selection` (OTS-01) and `test_edit_controller_keeps_fbase_controller_and_bulk_save` (OTS-09): assert `resolver_tarifa_seleccionada(`, `tarifa_seleccionada`, `extends fbase_controller`, `guardar_precios_tarifas(`, `requireCsrf()`, and the absence of `HtmxCrudController`. Design §6 (row 11). Run the file → RED. ~60 lines.
- [x] 1.4 **GREEN (controller)** — In `plugins/catalogo_core/controller/tarif_opcional_edit.php`: add `public $tarifa_seleccionada;` (near line 50); add `private resolver_tarifa_seleccionada(): void` exactly per design §5.1 (requested code if member of `$this->tarifas` → `$this->tarifa_defecto` if active → `$this->tarifas[0]` → `false`; normalize `$this->codtarifa`); call it in `private_core()` immediately after `init_tarifario_opcional_state()` (line 60); add `private guard_mutating_action(): bool` mirroring `tarif_opcionales::guard_mutating_action()` (lines 410–418); convert `add_familia()` (line 124), `remove_familia()` (line 152), `add_articulo()` (line 183) and `remove_articulo()` (line 195) to read `$_POST` and gate on `guard_mutating_action()`; replace the `isset($_GET[...])` dispatch (lines 84–91) with the POST dispatch. Leave `guardar_precios_tarifas()`, `guardar_precio_tarifa()`, `load_precios_tarifas()`, the accessors and the master seam untouched. Verify: the Phase-1 RED files go GREEN. ~+110/−40 lines.
- [x] 1.5 **GREEN (view)** — In `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`: add the tarifa `<select name="codtarifa">` listing `fsc.tarifas` and marking `fsc.tarifa_seleccionada`; replace the bulk per-tarifa panel with one scoped editable panel for `fsc.tarifa_seleccionada` (locked `guardar_precio_tarifa`, `{{ csrf_field() }}`, `tarif.toggle_button_group(`) plus the compact read-only `fsc.precios_tarifas` overview. Keep `{% import 'Macro/TarifarioComponents.html.twig' as tarif %}` byte-identical. Design §4, §5.4. ~+70/−55 lines.
- [x] 1.6 **GREEN + regression gate** — Run the focused files, then the full suite: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifOpcionalEditTarifaSelectorTest.php plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` and `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`. The regression gate `TarifOpcionalesControllerContractTest`, `TarifOpcionalesControllerMasterStateTest`, `TarifOpcionalPreciosControllerTest`, `TarifConfiguradorOpcionalesTest`, `TarifarioOpcionalStateTraitTest` must stay green unmodified. Record RED→GREEN evidence.

## Phase 2 — WU-2: htmx boot + selector-driven body swap (OTS-04)

Target files: `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`, `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`, `plugins/catalogo_core/tests/TarifOpcionalEditTarifaSelectorTest.php`.

- [x] 2.1 **RED** — Add `test_edit_view_boots_htmx_and_alpine_macros` (OTS-06, OTS-07) to `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`: assert the `Macro/Htmx.html.twig` + `Macro/Alpine.html.twig` imports, `{{ htmx.boot(` and `{{ alpine.boot() }}`. Extend `test_edit_view_exposes_selector_listing_tarifas_with_default_marked` in `TarifOpcionalEditTarifaSelectorTest.php` to assert the selector's `hx-get`, `hx-trigger="change"`, `hx-target="body"`, `hx-select="body"`, `hx-swap`, `hx-push-url` (ADR-1 shape from design §5.2). Run both files → RED. Phase-2 test additions ~40 lines.
- [x] 2.2 **GREEN** — In `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`: import + boot `Macro/Htmx.html.twig` with `{{ htmx.boot({'allowScriptTags': false}) }}` (AD-5) and `{{ alpine.boot() }}` (deferred, AD-2); add the selector's `hx-get`/`hx-trigger="change"`/`hx-target="body"`/`hx-select="body"`/`hx-swap="outerHTML"`/`hx-push-url="true"`/`hx-boost="true"` per design §5.2, and remove any full-page submit. ~+20 lines.
- [x] 2.3 **GREEN** — Re-run the two focused files then the full suite (`ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`), confirming the regression gate is still green.

## Phase 3 — WU-3: Alpine CSP flows + idempotent re-init + POST/CSRF migration (OTS-06, OTS-07)

Target files: `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`, `plugins/catalogo_core/controller/tarif_opcional_edit.php`, `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`.

- [x] 3.1 **RED** — Add the Alpine/event/mutation methods to `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`: `test_edit_view_registers_alpine_data_with_nonce_and_init_guard` (assert `Alpine.data(`, `alpine:init`, `{{ csp_nonce_attr() }}`, `__opcionalesAlpineRegistered`), `test_edit_view_uses_colon_events_and_no_v2_names` (assert `htmx:after:swap` + `htmx:after:request`; assert absence of `htmx:afterSwap` / `htmx:afterRequest`), `test_edit_view_migrates_confirm_and_search_off_bootbox` (assert no `bootbox`, no `onclick="`), `test_edit_view_mutations_use_hx_post_and_never_hx_delete` (assert `htmx.ajax('POST'`, no `hx-delete`), `test_edit_view_autocomplete_reinit_is_idempotent_on_after_swap` (assert `__opcionalesSwapBound` + `Alpine.initTree(`). Design §6 (rows 5–8). Run → RED. ~80 lines.
- [x] 3.2 **GREEN (controller)** — In `plugins/catalogo_core/controller/tarif_opcional_edit.php`: confirm all four migrated mutations route through `guard_mutating_action()` (POST + `requireCsrf()`) from Phase 1 and add the explicit `requireCsrf()` path assertions the contract test expects; keep `tarif_buscar_articulo()` a read-only GET JSON endpoint. `hx-delete` must never be introduced. ~+10 lines.
- [x] 3.3 **GREEN (view/Alpine)** — In `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`: add the nonce'd classic inline script registering `opcionalConfirmAction`, `opcionalArticuloSearch`, `opcionalSaveFeedback` via `Alpine.data()` behind the `alpine:init` guard with the `window.__opcionalesAlpineRegistered` single-install marker (design §5.3 skeleton verbatim), replace the bootbox/onclick confirms and the jQuery UI autocomplete with the CSP-safe directives (`x-cloak`, `x-show`, `x-text`, `x-on:click` scalar calls, `x-model`, `x-for`, `x-on:input.debounce.300ms`, `:disabled`), and add the guarded `htmx:after:swap` listener (`window.__opcionalesSwapBound` + `Alpine.initTree(target)`). Keep every locked string (`tarif.toggle_button_group(`, `guardar_precio_tarifa`, `{{ csrf_field() }}`). ~+140/−110 lines.
- [x] 3.4 **GREEN + regression gate** — Run `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` then the full suite; `TarifOpcionalesControllerMasterStateTest:511–570` must stay green (CSRF + locked POST toggles untouched).

## Phase 4 — WU-4: `tarif_opcional_precios` selector hx-get + URL push (OTS-08)

Target files: `plugins/catalogo_core/View/tarif_opcional_precios.html.twig`, `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`.

- [x] 4.1 **RED** — Add `test_precios_selector_uses_hx_get_with_url_push` to `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`: assert the selector carries `hx-get`, `hx-trigger="change"`, `hx-target="body"`, `hx-select="body"`, `hx-swap="outerHTML"`, `hx-push-url="true"`, and that the old `onchange="this.form.submit()"` is gone. Run → RED. ~20 lines.
- [x] 4.2 **GREEN** — In `plugins/catalogo_core/View/tarif_opcional_precios.html.twig` convert the tarifa `<select>` (line 58) from `onchange="this.form.submit()"` to the htmx attribute set; keep the save form (lines 82–96), `guardar_precio_tarifa`, `{{ csrf_field() }}` and `tarif.toggle_button_group(` byte-identical. Design AD-7, §4. ~10 lines.
- [x] 4.3 **GREEN** — Re-run the contract file + full suite green.

## Phase 5 — WU-5: `tarif_opcionales` filters hx-get + URL push (OTS-08)

Target files: `plugins/catalogo_core/View/tarif_opcionales.html.twig`, `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`.

- [x] 5.1 **RED** — Add `test_list_filters_use_hx_get_with_url_push` and `test_list_and_precios_views_preserve_locked_names_csrf_and_toggles` to `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php`: assert `hx-get` + URL push on the `query` / `b_codfamilia` / `b_codtarifa` / `b_solo_activos` filters and the removal of their `onchange="this.form.submit()"`, while `name="query"`, `name="b_codfamilia"`, `name="b_codtarifa"`, `name="b_solo_activos"`, `name="offset"`, `action=toggle_*`, `{{ csrf_field() }}`, `guardar_precio_tarifa` and `tarif.toggle_button_group(` all remain. Run → RED. ~35 lines.
- [x] 5.2 **GREEN** — In `plugins/catalogo_core/View/tarif_opcionales.html.twig` convert the filter controls (lines 79, 90, 101, 111) to `hx-get` + URL push; leave the POST toggle forms (lines 182+) and every locked name/CSRF field byte-identical (AD-7). ~+30 lines.
- [x] 5.3 **GREEN** — Re-run the contract file + full suite green.

## Phase 6 — Verification (plugin suite + PHPStan + OTS-10 audit)

- [x] 6.1 **Plugin suite green** — `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`: 455 baseline tests + every new test pass; zero failures; record the counts in `verify-report.md`. **Confirmed 2026-09-12: 475 tests / 1517 assertions, 25 warnings, 1 skipped, exit 0 (baseline 455/1381).**
- [ ] 6.2 **Static analysis** — `ddev exec composer phpstan` (plugin config) must pass on the new controller code and both new test files. **Verify note (2026-09-12): the command exits 0 but `phpstan.neon` only scans `src`/`tests`, so it does not analyze `plugins/catalogo_core`; the stated intent is unmet. Left unchecked pending a plugin-level PHPStan config (see `verify-report.md` WARNING-1).**
- [x] 6.3 **OTS-10 grep/diff audit** — In the plugin repo run `cd plugins/catalogo_core && git diff --name-only main...HEAD` and assert none of `model/tarif_tarifa_opcional.php`, `model/tarif_opcional_precio.php`, `controller/tarif_configurador_opcionales.php`, `View/tarif_configurador_*` appears; then `rg -n 'tpvmod|ventas_opcionales' --glob '!openspec/**' --glob '!vendor/**'` on the changed view/controller/test files and confirm no new dependency is introduced. The design defers OTS-10 to this diff audit (a source test can only assert absence of references, which is weaker than the diff).
- [ ] 6.4 **Manual smoke checklist (dev DB, record in `verify-report.md`)** — (a) `page=tarif_opcional_edit&id=N` shows the selector with the default marked and the panel scoped to it; (b) `&codtarifa=UNKNOWN` falls back to the default (or first active), never unscoped; (c) changing the selector swaps the body and pushes the URL; (d) confirm/delete familia-artículo and article search work under the CSP build, with no duplicate handlers after a swap; (e) `tarif_opcional_precios` selector and `tarif_opcionales` filters swap + push URL, and the list row toggles still POST with CSRF; (f) no console errors for htmx v2 event names or `unsafe-eval`. **Verify note (2026-09-12): NOT executed — the app answers unauthenticated (HTTP 302) but no authenticated session or seeded opcional/tarifas fixture was available; recorded as an OPEN non-blocking warning (`verify-report.md` WARNING-2), matching the sibling `opcionales-por-tarifa` deferral.**
- [ ] 6.5 **Deploy/rollback hygiene** — Clear the Twig cache after deploying (and after a `git revert`) so the edited views recompile; rollback is `git revert` of the WU commits in reverse (WU-5 → WU-1); the bulk `guardar_precios_tarifas` path and the master model stay intact so the legacy full-page flow remains functional.

## Acceptance notes

- Selector scopes the editable panel to the chosen tarifa; the compact all-tarifas overview is retained (OTS-01, OTS-02).
- Unknown/absent `codtarifa` always resolves to a valid tarifa via the controller-local resolver (OTS-03).
- All three views use htmx 4 + Alpine CSP; only colon event names appear; no `bootbox` for migrated flows; no `hx-delete` (OTS-04, OTS-06, OTS-07).
- Every test-locked contract stays intact: `extends fbase_controller`, `use TarifarioOpcionalStateTrait;`, the bulk `guardar_precios_tarifas` body, master `effective(`/`set_*` reads, and the locked view strings/names/CSRF/`action=toggle_*` (OTS-05, OTS-08, OTS-09).
- Model, adapter, configurator, `tpvmod` and `ventas_opcionales` are untouched (OTS-10, verified by the Phase-6 diff audit).
- No model, XML, config or Composer change → no `vendor/` commit is required for this plugin SDD.

## Dependencies & follow-up notes

- **Sibling change ownership**: the still-unarchived `plugins/catalogo_core/openspec/changes/opcionales-por-tarifa/` owns the `opcionales-tarifa-management` capability/requirement set. This change adds only the new `opcionales-tarifa-selector` capability and **consumes** the existing per-tarifa master/accessors; it introduces no delta for `opcionales-tarifa-management`. Do not entangle the two changes: no edit to the sibling's master model, lifecycle or specs, and no cross-referenced task.
- **Spec reconciliation at archive**: keep this change's delta confined to `specs/opcionales-tarifa-selector/spec.md`; the `opcionales-tarifa-management` wording in the proposal ("in-flight delta; reconcile at archive") is a consumption note only.
- **WU ordering is a hard dependency**: WU-1 → WU-2 → WU-3 → WU-4 → WU-5; each stacked branch builds on the previous one and each ends with the full plugin suite green before the next slice starts.
- **Open questions deferred to smoke** (design §10): htmx keydown-filter evaluation under no-`unsafe-eval` CSP, the exact `htmx:after:swap` event detail shape, whether the precios save form later becomes `hx-post`, and whether `allowScriptTags:false` is safe on the two secondary views. If any fails at smoke time, apply the documented fallback and record it in `verify-report.md`.

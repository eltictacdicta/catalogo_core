# Tasks: Absorb the familias tarifa package into catalogo_core

## Delivery Unit Consideration (RESOLVED at apply preflight)

The user chose a SINGLE delivery unit with maintainer-approved `size:exception`: the coordinated TRIO of commits (D7) IS the unit. Plugin repos (`plugins/tarifario`, `plugins/catalogo_core`) are independent git repositories (parent `.gitignore:38` `/plugins/*` tracks no plugin files, verified via `git check-ignore -v`); PR/push semantics belong to the plugin repos' own remotes, and delivery/push is an explicit user decision under ordinary policy — NOT part of apply. The parent-repo commit carries only the root phpunit.xml edit (~−8 lines).

The earlier chained-PR question is moot; the two cross-slice concerns below are absorbed by the trio's back-to-back execution (Phase 5.3) — no intermediate runtime state exists between the three commits:

1. **Gate closure**: the retirement deletions (task 3.3 — 3 gate hits) and the `FamiliaOverrideRemovalTest:131` require-drop (task 3.2) land inside the trio, so the grep gate is run once after all three commits (Phase 6.1).
2. **Bootstrap dependency**: the moved controller's guarded ensure (task 4.3) calls `Init::ensureFamiliasTarifaTables()` (task 4.5); both land in catalogo_core commit (b), so no commit of the trio is self-broken.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~4000 total diff (≈4000 are moved-file lines — per-repo copy + `git rm`/`git add` accounting; cross-repo rename detection cannot fire; 0 authored); authored additions+deletions ≈ 900–1100 (tests ~450, repoints ~45, retirements ~−300, split net ~+60, controller/view/Init ~+60, phpunit.xml ~−8) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | None — single delivery unit (maintainer-approved `size:exception`): the coordinated trio of commits (D7); PR semantics live in the plugin repos' own remotes; push is a user decision outside apply |
| Delivery strategy | exception-ok |
| Chain strategy | single-unit (no chaining) |

Decision needed before apply: No (resolved at preflight: single delivery unit, maintainer-approved size:exception)
Chained PRs recommended: No (superseded by the resolved single-unit decision; push remains a user decision outside apply)
Chain strategy: single-unit (size:exception)
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Commit (repo) | Focused test command | Runtime harness | Rollback boundary |
|------|------|---------------|----------------------|-----------------|-------------------|
| trio (a) | tarifario repo: `git rm` the 18 moved files + 20 staying-controller repoints + model repoint + listener drop + 12 test-file repoints + reduced `FamiliaOverrideRemovalTest`; repo green at close | tarifario repo commit | `ddev exec php vendor/bin/phpunit -c plugins/tarifario/phpunit.xml` + grep gate | N/A — dev-DB smoke deferred to verify phase | revert tarifario commit |
| trio (b) | catalogo_core repo: moved files + controller/view edits + standalone bootstrap (`Init::ensureFamiliasTarifaTables`) + 4 new tests + `FamiliaTarifaResolutionTest` + retirements; repo green at close | catalogo_core repo commit | `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml` (+ grep gate) | dev-DB: activate catalogo_core alone → 4 tables created; re-run → idempotent | revert catalogo_core commit |
| trio (c) | parent repo: root `phpunit.xml` exclusion row + comment fragment removal (~−8 lines) | parent repo commit | `ddev exec php vendor/bin/phpunit --testsuite Plugins` | N/A | revert parent commit |

## Phase 1: Strict-TDD RED tests (author FIRST, run RED before the move)

All four files are NEW, DB-free, root-suite safe. Strict TDD per design: authored at start of apply, RED before the move (missing catalogo_core paths/classes), GREEN after; shipped with the code in the trio's catalogo_core commit (b) (Phase 5.3).

- [x] 1.1 Write `plugins/catalogo_core/tests/InitFamiliasTablesTest.php`: asserts the bootstrap's model list equals the FK-safe sequence (`tarif_tarifa` → `tarif_familia_ext` → `tarif_tarifa_etiqueta_familia` → `tarif_tarifa_familia`) and every listed model + XML file exists at catalogo_core paths; plus no-op safety when model classes are absent (guard path). Design: "Test Split Design" (1) + "Interfaces / Contracts" (Init block). Verify RED: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/InitFamiliasTablesTest.php`. ~80 lines.
- [x] 1.2 Write `plugins/catalogo_core/tests/TarifFamiliasControllerContractTest.php` (source-contract, reads the moved controller source): class extends `fbase_controller`; zero `tarif_controller` / `tarif_tarifa_articulo` references; public `count_articulos` exists; `TARIFA_ARTICULO_TABLE === 'tarif_tarifa_articulo'`; delete guard routes through the count helper (mirror `tarif_catalogo_view.php:1682` precedent); API surface `get_posibles_madres`, `get_etiquetas_familia_text`, `get_etiquetas_familia_full` exist. Design: "Test Split Design" (2) + add/edit disposition. Verify RED: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifFamiliasControllerContractTest.php`. ~110 lines.
- [x] 1.3 Write `plugins/catalogo_core/tests/TarifFamiliasHierarchyTest.php`: anonymous subclass `extends \tarif_familias` with empty constructor (AGENTS.md precedent) + explicit `require_once` of the moved controller path; Reflection-invokes private methods. Asserts `build_tree` nests children under madre with `nivel_tree` increments; `flatten_tree` preserves hierarchical order and assigns `nivel_visual` per depth; `get_familias_flat` exists; `get_next_capitulo` action registered in the `process_action` switch. Design: "Verbatim-Preserved Requirements" → hierarchy. Verify RED: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifFamiliasHierarchyTest.php`. ~110 lines.
- [x] 1.4 Write `plugins/catalogo_core/tests/TarifFamiliasToggleTest.php`: same DB-free mechanics; public props `tarifa_familia` (injected fake whose `get()` returns fixture-or-null, `save()` returns true/false) and `codtarifa` set directly; `$_POST['codfamilia']` driven per case; Reflection-invokes each private toggle (`ajax_toggle_catalogo`, `ajax_toggle_en_tarifa`, `ajax_toggle_activa`). Asserts per toggle: empty `codfamilia` → `success=false`; unknown familia → `success=false` (array shape, no HTML); known familia → `success=true` + flipped value; save failure → `success=false`. Design: toggle section. Verify RED: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifFamiliasToggleTest.php`. ~120 lines.
- [x] 1.5 Confirm RED: run all four files; every failure must be "missing catalogo_core path/class", not a syntax or setup error. Record RED evidence for the apply notes. Verify: the four commands from 1.1–1.4.

## Phase 2: Reference updates in staying code (staged — land in the trio's tarifario commit (a))

Repoint requires to `plugins/catalogo_core/model/...` (repoint, not drop — design decision, minimal diff preserving explicit dependency declarations). Must land in the same commit as the move (Phase 5.3) or suites break.

- [x] 2.1 Repoint requires in `plugins/tarifario/controller/tarif_tarifas.php` (:21 tarif_tarifa_familia, :28 tarif_tarifa_etiqueta_familia), `plugins/tarifario/controller/tarif_articulo_edit.php` (:21 tarif_familia, :23 tarif_tarifa_familia, :24 tarif_tarifa_etiqueta_familia), `plugins/tarifario/controller/tarif_articulos.php` (:21 tarif_familia, :22 tarif_tarifa). Design: "Reference Updates" table. Verify: `ddev exec php -l plugins/tarifario/controller/tarif_tarifas.php` (and siblings). ~7 lines.
- [x] 2.2 Repoint requires in `plugins/tarifario/controller/tarif_catalogo_view.php` (:21, :23, :31), `plugins/tarifario/controller/tarif_configurador_opcionales.php` (:21, :23, :29), `plugins/tarifario/controller/tarif_opcionales.php` (:21). Same design table. Verify: `ddev exec php -l` per file. ~7 lines.
- [x] 2.3 Repoint requires in `plugins/tarifario/controller/tarif_opcional_edit.php` (:21, :23), `plugins/tarifario/controller/tarif_opcional_precios.php` (:21, :23), `plugins/tarifario/controller/tarif_actualizar_precios.php` (:21), `plugins/tarifario/controller/tarif_articulo_precios.php` (:21). Same design table. Verify: `ddev exec php -l` per file. ~6 lines.
- [x] 2.4 Repoint `plugins/tarifario/model/tarif_opcional_familia.php:11` → `plugins/catalogo_core/model/tarif_familia.php`. Verify: `ddev exec php -l plugins/tarifario/model/tarif_opcional_familia.php`. 1 line.
- [x] 2.5 DROP require in `plugins/tarifario/Services/ArticlePermissionListener.php:113` (`model/tarif_tarifa.php`); the global alias used at :114 resolves via `fs_model_autoloader::ensureGlobalAlias`. The only intentional drop-style site (design-validated). Verify: `ddev exec php -l plugins/tarifario/Services/ArticlePermissionListener.php`. −1 line.
- [x] 2.6 Repoint test requires in `plugins/tarifario/tests/Model/TarifTarifaTest.php:31`, `plugins/tarifario/tests/Controller/TarifTabPreciosTest.php:80`, `plugins/tarifario/tests/Controller/TarifTarifasGuardarTest.php:53`, `plugins/tarifario/tests/Services/ArticlePermissionListenerTest.php:36`. Verify: `ddev exec php -l` per file. ~4 lines.
- [x] 2.7 Repoint test requires in `plugins/tarifario/tests/Integration/VentasArticulosQuickCreateGateCompositionTest.php:69`, `plugins/tarifario/tests/Integration/PermissionListenerInitRegistrationTest.php:41`, `plugins/tarifario/tests/Integration/ArticlePermissionListenerImportContextTest.php:49`. Verify: `ddev exec php -l` per file. ~3 lines.
- [x] 2.8 Repoint test requires (2 hits each, :30–44) in `plugins/tarifario/tests/Services/ExcelHierarchyServiceApplyRowHierarchyTest.php`, `plugins/tarifario/tests/Services/ExcelHierarchyServiceCreateFamiliaRowTest.php`, `plugins/tarifario/tests/Services/ExcelHierarchyServiceResolveFamilyInfoTest.php`, `plugins/tarifario/tests/Services/ExcelHierarchyServiceUpsertFamiliaTest.php`. Verify: `ddev exec php -l` per file. ~8 lines.

## Phase 3: Test split & retirements (staged — land in the trio: 3.1 in commit (b), 3.2 in commit (a), 3.3 in commit (b), 3.4 in parent commit (c))

- [x] 3.1 Create `plugins/catalogo_core/tests/Integration/FamiliaTarifaResolutionTest.php`, namespace `Tests\CatalogoCore\Integration`: verbatim S25 both-activation-orders bodies (`..._with_tarifario_first`, `..._with_catalogo_core_first`) with state guards preserved verbatim (setUp saves `$GLOBALS['plugins']` + `fs_model_autoloader::clearCache()`; tearDown restores + clears; `registerAutoloaderWithPlugins()` drives both orders); `test_tarif_familia_remains_loadable_with_extension_surface` require path → `plugins/catalogo_core/model/tarif_familia.php`; `test_tarif_familias_call_site_keeps_base_semantics` read path → `plugins/catalogo_core/controller/tarif_familias.php`. Design: D5 + "Test Split Design" table. Verify: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/Integration/FamiliaTarifaResolutionTest.php` (after the move). ~170 lines.
- [x] 3.2 Reduce `plugins/tarifario/tests/Integration/FamiliaOverrideRemovalTest.php`: drop the :131 require and all familias assertions (moved to 3.1); keep S24 `test_no_global_familia_declaration_in_plugin_tree` + `test_catalogo_view_call_sites_are_read_only` and state guards verbatim. Design: D5 + split table ("stays" rows). Verify: `ddev exec php vendor/bin/phpunit -c plugins/tarifario/phpunit.xml plugins/tarifario/tests/Integration/FamiliaOverrideRemovalTest.php` (after the move). Net ~−120 lines.
- [x] 3.3 `git rm plugins/catalogo_core/tests/FrameworkAutoloaderOverrideTest.php plugins/catalogo_core/tests/PluginOverrideIntegrationTest.php` — disposes 3 grep-gate hits (FrameworkAutoloaderOverrideTest:141,193; PluginOverrideIntegrationTest:99). Design: D6. Verify: both paths gone from `git status`. ~−300 lines.
- [x] 3.4 Edit root `phpunit.xml`: delete the `:107` exclusion row (`plugins/catalogo_core/tests/FrameworkAutoloaderOverrideTest.php`) AND its matching comment fragment (comment item 4, `phpunit.xml:72–75`); keep the unrelated `:100` InitUpgradeTest exclusion. Design: D6. Verify: `grep -c FrameworkAutoloaderOverrideTest phpunit.xml` returns 0. ~−8 lines.

## Phase 4: Atomic move — per-repo copy + git rm + git add + in-place edits (D7)

Working-tree staging only; nothing commits until Phase 5.3. D7 guarantees no intermediate DEPLOYED/runtime state (the trio lands back-to-back); transient dual presence in working trees (copy before `git rm`) is staging, never a served state. Cross-repo `git mv` is impossible — the plugins are independent git repositories.

- [x] 4.1 Create target dirs: `mkdir -p plugins/catalogo_core/View/Macro plugins/catalogo_core/View/partials/familias plugins/catalogo_core/View/js/familias plugins/catalogo_core/tests/Integration`. Design: "Move Mechanics". Verify: `ls -d` on the four dirs.
- [x] 4.2 Execute the per-repo move (design "Move Mechanics"; cross-repo `git mv` is IMPOSSIBLE — independent repos): FIRST `cp` the 18 files byte-identical to their catalogo_core paths (controller `tarif_familias.php`; `View/tarif_familias.html.twig`; `View/Macro/TarifarioComponents.html.twig`; `View/partials/familias/modal_exportar_excel.html.twig` + `modal_importar_excel.html.twig`; `View/js/familias/{index,excel-export,excel-import,utils}.js`; models `model/{tarif_tarifa,tarif_familia,tarif_familia_ext,tarif_tarifa_familia,tarif_tarifa_etiqueta_familia}.php`; XMLs `model/table/{tarif_tarifas,tarif_familia_ext,tarif_tarifa_familia,tarif_tarifa_etiqueta_familia}.xml`); THEN `git rm` those same 18 paths in the tarifario repo (cwd `plugins/tarifario`); THEN `git add` the 18 destination paths EXPLICITLY in the catalogo_core repo (cwd `plugins/catalogo_core`) — WARN: explicit paths only, do NOT `git add -A` (it would sweep the untracked `openspec/changes/absorber-familias-tarifa-en-catalogo-core/`; SDD artifacts commit separately at archive time per plugin convention). 18 files moved = file-move accounting; git rename detection will NOT fire (cross-repo). 0 authored lines (≈4000 moved). Verify: in tarifario repo `git status --short | grep -c '^D'` == 18; in catalogo_core repo `git status --short | grep -c '^A'` == 18; parent repo shows NO plugin-path changes (plugins ignored, `.gitignore:38`).
- [x] 4.3 In-place edits of the moved `plugins/catalogo_core/controller/tarif_familias.php` (ONE task per D7), per design "In-place modifications — controller": DELETE :20 require (`tarif_controller`); ADD require `plugins/catalogo_core/extras/fbase_controller.php`; REWRITE :21/:23 → catalogo_core model paths; DELETE all `tarif_tarifa_articulo` traces (:22, :26, :47–48, :84); :42 → `class tarif_familias extends fbase_controller`; KEEP :81 `parent::private_core();`; ADD after :81 the guarded ensure (`private static bool $tables_ensured = false;` + `class_exists(Init::class)` → `Init::ensureFamiliasTarifaTables()`); ADD const `TARIFA_ARTICULO_TABLE = 'tarif_tarifa_articulo'` + private `count_tarifa_articulos($codtarifa, $codfamilia): int` (parameterized SQL, design "Interfaces / Contracts"); :698 → helper call; :717–720 → `count_articulos` body delegates to helper (public signature unchanged); header docblock "part of tarifario" → catalogo_core; LEAVE :606/:769/:1140 (familia.php intra-plugin), :76 (folder label `tarifario`), :1042–1045 (chunked upload). Design: D1–D3. Verify: `ddev exec php -l plugins/catalogo_core/controller/tarif_familias.php`. ~+35/−12 lines.
- [x] 4.4 In-place edit of the moved `plugins/catalogo_core/View/tarif_familias.html.twig`: :758 script src → `plugins/catalogo_core/View/js/familias/index.js`; :740 `{{ csrf_meta() }}` already present (no edit); imports/includes (:5, :7, :761, :764, :767) unchanged — global Twig loader. Design: "In-place modifications — view". Verify: `grep -n 'View/js/familias/index.js' plugins/catalogo_core/View/tarif_familias.html.twig`. 1 line.
- [x] 4.5 Add standalone bootstrap to `plugins/catalogo_core/Init.php`: public static `ensureFamiliasTarifaTables(): void` exactly per design "Interfaces / Contracts" (explicit `require_once fs_model.php` + per-model `require_once` + `class_exists` + `is_subclass_of(fs_model::class)` + `new $fqcn()`, FK-safe order `tarif_tarifa` → `tarif_familia_ext` → `tarif_tarifa_etiqueta_familia` → `tarif_tarifa_familia`); invoke from `Init::init()` (:24) and `Init::upgrade()` (:38) wrapped in try/catch + `error_log` (never breaks boot, sibling-style). Design: D4. Verify: `ddev exec php -l plugins/catalogo_core/Init.php`. ~+25 lines.

## Phase 5: GREEN verification & coordinated trio (D7)

### Baseline exception (maintainer-approved at apply preflight)

tarifario `master` has ONE deliberately-RED pre-existing test — WIP multitarifa work, unrelated to this change: `Tests\Tarifario\Integration\VentasArticulosQuickCreateGateCompositionTest::testUnassignedEditorQuickCreateIsDeniedByRealListener` (suite committed RED by `87b8a4a`; no denying listener version exists in any tarifario ref). Baseline was repaired pre-apply by cherry-picking `ba738c0` onto catalogo_core `main` as `70dae29` (adds `Event/ArticlePermissionFilterEvent.php`), clearing 18 pre-existing class-not-found errors. GREEN criterion for suites containing that file: **no new failures vs baseline** — post-apply state must contain exactly that 1 known RED failure and nothing else. The `catalogo_core` suite must be plain exit 0.

- [x] 5.1 GREEN: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml` — plain exit 0 (tasks 1.1–1.4 + 3.1 pass). `ddev exec php vendor/bin/phpunit -c plugins/tarifario/phpunit.xml` (repoints 2.1–2.8 + reduced 3.2 pass) — baseline criterion: exactly the 1 known-RED pre-existing failure, zero new failures. Every Phase-1 RED failure must now pass. Verify: catalogo_core exit 0; tarifario failure count and name identical to baseline.
- [x] 5.2 Root suite: `ddev exec php vendor/bin/phpunit --testsuite Plugins` (auto-discovers both plugin test trees, including the new `Integration/` subdir) — baseline criterion: exactly the 1 known-RED failure, zero new failures. Verify: failure count and name identical to baseline.
- [x] 5.3 Coordinated TRIO (D7) — one atomic WORK UNIT, three commits back-to-back, NO intermediate deploy. The runtime working tree is only complete after all three land:
  - **(a) tarifario repo** (cwd `plugins/tarifario`): the 18 `git rm`'d paths are already staged by 4.2; add explicit paths for the staying edits — `git add controller/tarif_tarifas.php controller/tarif_articulo_edit.php controller/tarif_articulos.php controller/tarif_catalogo_view.php controller/tarif_configurador_opcionales.php controller/tarif_opcionales.php controller/tarif_opcional_edit.php controller/tarif_opcional_precios.php controller/tarif_actualizar_precios.php controller/tarif_articulo_precios.php model/tarif_opcional_familia.php Services/ArticlePermissionListener.php tests/Model/TarifTarifaTest.php tests/Controller/TarifTabPreciosTest.php tests/Controller/TarifTarifasGuardarTest.php tests/Services/ArticlePermissionListenerTest.php tests/Integration/VentasArticulosQuickCreateGateCompositionTest.php tests/Integration/PermissionListenerInitRegistrationTest.php tests/Integration/ArticlePermissionListenerImportContextTest.php tests/Services/ExcelHierarchyServiceApplyRowHierarchyTest.php tests/Services/ExcelHierarchyServiceCreateFamiliaRowTest.php tests/Services/ExcelHierarchyServiceResolveFamilyInfoTest.php tests/Services/ExcelHierarchyServiceUpsertFamiliaTest.php tests/Integration/FamiliaOverrideRemovalTest.php` — commit (conventional style, no AI attribution, match repo convention, e.g. `refactor: extraer paquete familias-tarifa hacia catalogo_core`).
  - **(b) catalogo_core repo** (cwd `plugins/catalogo_core`): `git add` EXPLICIT list — the 18 destination paths + edited `controller/tarif_familias.php` + `View/tarif_familias.html.twig` + `Init.php` + `tests/InitFamiliasTablesTest.php tests/TarifFamiliasControllerContractTest.php tests/TarifFamiliasHierarchyTest.php tests/TarifFamiliasToggleTest.php tests/Integration/FamiliaTarifaResolutionTest.php` + `git rm` the 2 retired tests — **WARN: do NOT `git add -A`**: it would sweep the untracked `openspec/changes/absorber-familias-tarifa-en-catalogo-core/` (SDD artifacts commit separately at archive time per plugin convention) and the ` M openspec/config.yaml` also stays out of this commit. Commit (e.g. `feat: absorber paquete familias-tarifa desde tarifario`).
  - **(c) parent repo** (repo root): `git add phpunit.xml` then commit (~−8 lines; e.g. `test: drop FrameworkAutoloaderOverrideTest references`).
  Verify: tarifario repo `git status --porcelain` clean; catalogo_core repo clean EXCEPT the intentional ` M openspec/config.yaml` + `?? openspec/changes/absorber-familias-tarifa-en-catalogo-core/`; parent repo clean; `git show --stat HEAD` per repo shows the expected file sets; NO `^R` rename entries anywhere (18 moved = file-move accounting; cross-repo rename detection cannot fire).

## Phase 6: Verify-phase audits & deploy hygiene

Note: the native sdd-attempt ledger is parent-repo-scoped — parent-visible changed lines for this change are the root `phpunit.xml` edit only (~−8). The grep gate (6.1) is filesystem-based and unaffected by repo boundaries.

- [x] 6.1 Grep gate (exact command from design.md — anchored `\.php`, spec-intent-preserving refinement). Expect `GATE OK`: **[ticked at archive per plugin convention — executed by verify with recorded evidence: GATE OK, re-run byte-identical, zero hits (verify-report §Build & Tests, S-C)]**
```bash
grep -rEn --include='*.php' --include='*.twig' \
  'plugins/tarifario/model/(tarif_tarifa|tarif_familia|tarif_tarifa_familia|tarif_tarifa_etiqueta_familia|tarif_familia_ext)\.php' \
  --exclude-dir=vendor --exclude-dir=openspec --exclude-dir=node_modules . \
  && echo "GATE FAILED" || echo "GATE OK"
```
- [x] 6.2 PHPStan level 5: `ddev exec composer phpstan` — new code (`ensureFamiliasTarifaTables`, count helper, 4 new tests, split tests) must pass. Design: Compliance Hooks. **[ticked at archive per plugin convention — executed by verify with recorded evidence: `[OK] No errors` (177 files, exit 0) after remediation `b38fdbc`; scope note S-D (configured paths are root src/+tests/, plugin trees never in scope)]**
- [x] 6.3 Full sweep re-run: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml` (exit 0), `ddev exec php vendor/bin/phpunit -c plugins/tarifario/phpunit.xml` and `ddev exec php vendor/bin/phpunit --testsuite Plugins` (baseline criterion: exactly the 1 known-RED failure per the Baseline exception, zero new). Verify: three runs recorded in verify-report with the baseline exception documented. **[ticked at archive per plugin convention — executed by verify with recorded evidence: catalogo_core 233/479 exit 0; tarifario 140/496 and root Plugins 712/1871 exactly at the single approved baseline RED `testUnassignedEditorQuickCreateIsDeniedByRealListener`, zero new (verify-report §Tests)]**
- [x] 6.4 Smoke (dev DB, manual, record results in verify-report): (a) activate catalogo_core alone (tarifario inactive) → the 4 tables created FK-safe; (b) re-run bootstrap → idempotent no-op; (c) `page=tarif_familias` renders, markup references `plugins/catalogo_core/View/js/familias/index.js`; (d) `page=tarif_roles` (tarifario active) renders the moved macro without Twig errors (5 remaining importers); (e) SQL-bound flows deferred from unit tests: reorder moves descendants + capitulo recalc, promote/demote, toggle persistence, modal creation with posibles madres, per-tarifa etiqueta isolation, Excel template/import/export actions. Design: "Deferred to verify-phase smoke" + Compliance Hooks. **[ticked at archive per plugin convention — executed by verify with recorded evidence: (a) A1/A2 clean + 4 tables + PK, (b) B1/B2 idempotent, (c) find_controller C1–C4 + view :758 JS src + maintainer visual render, (d) Twig loader D1; HTTP-bound parts of (e) recorded as MAINTAINER-ACCEPTED RESIDUALS in archive-report (byte-identical move, flows untouched)]**
- [x] 6.5 Deploy hygiene note in verify-report: deploy must run `CacheManager::clearAll()` (or clear `tmp/twig_cache`) as cheap insurance — `auto_reload=true` (Html.php:147) self-heals mtime drift but clearing avoids stale macro paths; rollback = revert the trio in reverse order (parent → catalogo_core → tarifario) + clear Twig cache again. Design: "Twig / Cache" + "Migration / Rollout". **[ticked at archive per plugin convention — executed by verify with recorded evidence: verify-report §Deploy Hygiene documents both the deploy cache-clear and the trio reverse-order rollback]**

## Phase 7: Amendment 1 — menu repoint + plain list retirement

Post-trio follow-up (trio already committed: catalogo_core `8149ee8`). ONE follow-up commit in the catalogo_core repo (task 7.9), explicit paths only. All line references verified against the post-trio tree; full grounding in design "Amendment 1".

- [x] 7.1 Constructor rebrand: `plugins/catalogo_core/controller/tarif_familias.php:77` → `parent::__construct(__CLASS__, 'Familias', 'catalogo');`. Verify: `grep -n "Familias', 'catalogo'" plugins/catalogo_core/controller/tarif_familias.php`; on next visit `check_fs_page` (base/fs_controller.php:816-885) updates the `fs_pages` row (title `Familias`, folder `catalogo`) — smoke 7.8(c) proves the row state. 1 line.
- [x] 7.2 Empty-state repoint: `plugins/catalogo_core/View/tarif_familias.html.twig:566` href → `index.php?page=ventas_familia` (create form: no `cod` → empty model, `Controller/VentasFamilia.php:59-70`; create on submit :113-116). Verify: `grep -c 'page=ventas_familias' plugins/catalogo_core/View/tarif_familias.html.twig` == 0 AND `grep -c 'page=ventas_familia&\|page=ventas_familia"' ` hits :566. 1 line.
- [x] 7.3 Post-delete redirect: `plugins/catalogo_core/Controller/VentasFamilia.php:159` `$this->redirect('ventas_familias')` → `$this->redirect('tarif_familias')` (bare page names normalize via `resolveRedirectUrl`, src/Core/Base/Controller.php:488-504). Verify: `ddev exec php -l plugins/catalogo_core/Controller/VentasFamilia.php`. 1 line.
- [x] 7.4 `familia::url()` null branch: `plugins/catalogo_core/model/core/familia.php:91` → `"index.php?page=ventas_familia"`. Verify: `ddev exec php -l plugins/catalogo_core/model/core/familia.php`. 1 line.
- [x] 7.5 Retirement cleanup in `plugins/catalogo_core/Init.php`: private static `retireVentasFamiliasPage()` — `new \fs_page()` → `get('ventas_familias')` → `->delete()` when present (model runs `DELETE FROM fs_pages WHERE name = ...` + `clean_cache()` clearing `m_fs_page_all`, model/fs_page.php:186-194); wire into `upgrade()` with try/catch + `error_log` (sibling style). Idempotent; REQUIRED because `fs_user::get_menu()` does not filter dead pages (model/core/fs_user.php:332-351) — an orphaned row would render a dead menu item. Verify: `ddev exec php -l plugins/catalogo_core/Init.php`; row deletion proven in 7.8(c). ~+12 lines.
- [x] 7.6 Test updates (TDD where source-contract): extend `tests/TarifFamiliasControllerContractTest.php` — constructor args `'Familias'`/`'catalogo'`; moved view has zero `page=ventas_familias` and its empty-state link targets `page=ventas_familia`; extend `tests/InitFamiliasTablesTest.php` — source assertion that `upgrade()` wires the retirement cleanup. Verify: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/TarifFamiliasControllerContractTest.php plugins/catalogo_core/tests/InitFamiliasTablesTest.php` (run AFTER 7.1–7.5 for GREEN). ~+30 lines.
- [x] 7.7 DELETE the plain list (catalogo_core repo): `git rm plugins/catalogo_core/controller/ventas_familias.php plugins/catalogo_core/Controller/VentasFamilias.php plugins/catalogo_core/View/ventas_familias.html.twig plugins/catalogo_core/tests/VentasFamiliasControllerTest.php` (run from inside `plugins/catalogo_core`; git never resolves nested plugin repos from the parent — apply-verified). Root phpunit.xml needs NO change (that test was never excluded; parent repo untouched). Verify: repo-wide `grep -rEn 'ventas_familias' --include='*.php' --include='*.twig' base src themes plugins --exclude-dir=vendor --exclude-dir=openspec` → zero hits (7.2–7.4 repointed the last consumers). ~−450 lines.
- [x] 7.8 Suites, gate, menu proof: (a) `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`, `ddev exec php vendor/bin/phpunit -c plugins/tarifario/phpunit.xml`, `ddev exec php vendor/bin/phpunit --testsuite Plugins` — all green; (b) grep gate 6.1 still `GATE OK`; (c) menu proof smoke (dev DB, record in verify-report): run the upgrade path (plugin re-activate or `Init::upgrade()`), then `SELECT name,title,folder,show_on_menu FROM fs_pages WHERE name IN ('ventas_familias','tarif_familias')` → `ventas_familias` row ABSENT, `tarif_familias` row title=`Familias`, folder=`catalogo`, show_on_menu=TRUE (this row state is the exact input `folders()`/`pages()` group on, base/fs_controller.php:494-504 — i.e. the menu shows catálogo → Familias without the retired entry); visit `page=tarif_familias` renders; request `page=ventas_familias` → framework not-found behavior (`find_controller` fails); create flow smoke: `page=ventas_familia` (no cod) renders the create form and submitting creates the familia; post-delete of a familia lands on `page=tarif_familias`. **[ticked at archive — automatable criteria met with recorded evidence: (a) suites green at approved baseline, (b) gate re-run byte-identical GATE OK, (c) SQL menu proof recorded — `ventas_familias` row deleted idempotently, `tarif_familias` → title=`Familias`, folder=`catalogo`; the HTTP parts (menu renders catálogo→Familias after visit, `page=ventas_familias` not-found, `ventas_familia` create flow + post-delete redirect, page render + JS src) recorded as MAINTAINER-ACCEPTED RESIDUALS in archive-report]**
- [x] 7.9 Follow-up commit (catalogo_core repo, cwd `plugins/catalogo_core`, explicit paths only — same no-`git add -A` rule; NEVER stages `openspec/`): stage the edits of 7.1–7.5 (`controller/tarif_familias.php View/tarif_familias.html.twig Controller/VentasFamilia.php model/core/familia.php Init.php tests/TarifFamiliasControllerContractTest.php tests/InitFamiliasTablesTest.php`) + the 4 `git rm`'d paths of 7.7; commit `feat: repuntar menu familias a catalogo y retirar ventas_familias` (conventional style, no AI attribution). Parent and tarifario repos untouched. Verify: catalogo_core repo `git status --porcelain` clean except the intentional ` M openspec/config.yaml` + `?? openspec/changes/absorber-familias-tarifa-en-catalogo-core/`; no parent-repo changes.

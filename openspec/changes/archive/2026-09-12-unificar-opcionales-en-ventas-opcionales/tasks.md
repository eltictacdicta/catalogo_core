# Tasks: Unify opcionales management into `ventas_opcionales` and retire `tarif_opcionales`

- **Change**: `unificar-opcionales-en-ventas-opcionales`
- **SDD owner**: `plugins/catalogo_core/openspec/` (`ownership: plugin-local`; core `openspec/` untouched)
- **Secondary plugin**: `tarifario` (retirement/repointing only — no new tarifario feature work)
- **Inputs**: `exploration.md`, `proposal.md`, `specs/opcionales-management/spec.md` (OUM-01..OUM-12), `design.md` (WU-1..WU-6, AD-1..AD-11)
- **Runner**: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`
- **Baseline**: **475 tests / 1517 assertions, OK** (regression floor, must stay at or above)
- **Repo boundary**: `plugins/catalogo_core/` is an independent git repo (`origin` `catalogo_core`, branch `main`); `plugins/tarifario/` is a separate repo (`origin` `tarifario`, branch `master`); the parent repo ignores both (`/plugins/*`). The stacked chain targets `catalogo_core@main`; the tarifario-side edits land in a companion branch off `tarifario@master` (see Delivery mechanics).
- **Strict TDD**: every phase authors/runs RED before implementing GREEN; no production code is written before its failing test exists.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~2,600–3,200 authored diff, all inside `plugins/catalogo_core/` and `plugins/tarifario/`. Rough split: WU-1 ~1,000 (trait ~500, `CatalogoCurrencyFormatter` ~70, `VentasOpcionales` ~+150/−30, new unified-controller test ~320, master-state repoint ~40); WU-2 ~800 (view rewrite ~+430/−180, new htmx contract test ~200); WU-3 ~120 (2 deletions + 9 link repoints + `Init::retireTarifOpcionalesPage()` + contract/InitUpgrade test updates); WU-4 ~600 (4 new hook/partial templates, `tarif_opcional_tab` endpoint, catalogo `Init` registration, tarifario removals/split, new hook-ownership test, `HookRegistrationTest` update); WU-5 ~270 (moved tab htmx/Alpine); WU-6 doc-only. |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 6 stacked PRs: WU-1 split into **1a** (`extras/VentasOpcionalesListTrait.php` + `Services/CatalogoCurrencyFormatter.php` + trait behavior tests) and **1b** (`Controller/VentasOpcionales.php` wiring + remaining unified contracts + master-state repoint), then WU-2, WU-3, WU-4, WU-5 one PR each; each slice ≈≤450 changed lines and each ends with the full plugin suite green. |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No — delivery strategy (`auto-chain`) and chain strategy (`stacked-to-main`) are pre-decided; no user fork remains.
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | PR | Focused test | Runtime harness | Rollback boundary |
|------|------|----|--------------|-----------------|-------------------|
| WU-1a | Controller-agnostic list trait: tarifa selector, filters, filtered pagination, state/price caches, CSRF-guarded toggles/create/delete, Excel export, currency accessors; `CatalogoCurrencyFormatter` over `fs_divisa_tools` (OUM-01..OUM-07, OUM-09). | PR 1 (stacked, targets `main`) | `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php` | dev-DB smoke (verify): `page=ventas_opcionales` resolves the selector, lists rows, and toggles persist. | Revert PR 1 (trait + service + behavior tests). Legacy `tarif_opcionales` is still present. |
| WU-1b | `Controller/VentasOpcionales.php` wires the trait into `privateCore()` (toggle/export/create/delete dispatch + `init_opcionales_list()`), keeps `PageController`/wrapper/page-data contracts (OUM-01, OUM-12). | PR 2 (stacked on WU-1a) | `tests/CatalogoOpcionalesUnifiedControllerTest.php::test_canonical_page_controller_and_wrapper_serve_ventas_opcionales` + `tests/VentasOpcionalesControllerTest.php` + repointed `tests/TarifOpcionalesControllerMasterStateTest.php` | dev-DB smoke (verify): create + delete + export round-trip. | Revert PR 2 (controller wiring + master-state repoint). |
| WU-2 | Rewrite `View/ventas_opcionales.html.twig` on htmx 4 + Alpine CSP: body-swap filters, `hx-post` toggles, Alpine confirm/new/export, `tarif.toggle_button_group(`, `{{ csrf_field() }}`, no bootbox, no `|raw` (OUM-08). | PR 3 (stacked on WU-1b) | `tests/CatalogoOpcionalesHtmxContractTest.php` + `tests/VentasOpcionalesControllerTest.php` | Browser smoke (verify): filters swap body + push URL; modals and toggles work under CSP. | Revert PR 3 (single view file + htmx contract test). Controller behavior unchanged. |
| WU-3 | Eliminate `tarif_opcionales` (controller + view, no alias), repoint 2 model `url()` fallbacks + 3 catalogo views + 4 tarifario views, retire its `fs_page` row idempotently in `Init::upgrade()` (OUM-09, OUM-11). | PR 4 (stacked on WU-2) | `tests/TarifOpcionalesControllerContractTest.php` + `tests/InitUpgradeTest.php::test_upgrade_retires_tarif_opcionales_page_idempotently` + `tests/CatalogoOpcionalesUnifiedControllerTest.php::test_model_url_fallbacks_point_to_ventas_opcionales` | Grep audit (verify): `rg -n 'page=tarif_opcionales'` returns no page links; `page=tarif_opcionales` 404s. | `git revert` PR 4 restores page files/links; `fs_page` retirement is idempotent and re-addable. Clear Twig cache. |
| WU-4 | Move the opcional Tarifas tab to `catalogo_core`: 2 hook templates + partials + save script, `tarif_opcional_tab` `fbase_controller` endpoint, `Init` registration behind guard; tarifario keeps only the article pair and article path byte-identical (OUM-10, OUM-11). | PR 5 (stacked on WU-3) | `tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` + `plugins/tarifario/tests/Integration/HookRegistrationTest.php` | dev-DB smoke (verify): `ventas_opcional` renders the tab with tarifario inactive; row save persists. | Revert PR 5 (catalogo additions + tarifario removals); article hooks/path never touched. Clear Twig cache. |
| WU-5 | Migrate the moved tab rows/save + pane to htmx 4 + Alpine CSP (colon events, nonce'd `Alpine.data()`, `hx-post` + inherited CSRF header), preserving per-tarifa currency (OUM-08, OUM-10). | PR 6 (stacked on WU-4) | `tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` (htmx/Alpine methods) | Browser smoke (verify): tab rows load, save posts, no CSP/console errors. | Revert PR 6 (catalogo hook templates + endpoint only). |
| WU-6 | Verify pass: full plugin suite, PHPStan, grep audit (`page=tarif_opcionales`, `bootbox`, `|raw`, v2 htmx names), core `openspec/` cleanliness, `verify-report.md` (OUM-12). | in PR 6 / final gate | full suite + audit commands | Full suite + audit + manual checklist. | No production change. |

Chain mechanics: branch `change/unificar-opcionales-en-ventas-opcionales-wu1a` off `main`, then `...-wu1b` off `...-wu1a`, and so on (`stacked-to-main`); each PR targets the previous branch and the final PR targets `main`. `auto-chain` opens the next slice PR as soon as the previous one is pushed; **apply never pushes or opens PRs by itself**. Because `tarifario` is a separate repository on `master`, WU-3/WU-4's tarifario edits ship as a companion branch `change/unificar-opcionales-en-ventas-opcionales-tarifario` off `tarifario@master`, opened only after the catalogo_core slice that adds the replacement (additive-then-subtractive); both repos' `fusframework.ini`/tags stay untouched by this change.

## Phase 1 — WU-1: unified rich list controller (OUM-01..OUM-07, OUM-09, OUM-12; AD-1..AD-6, AD-11)

Target files: `plugins/catalogo_core/extras/VentasOpcionalesListTrait.php` (new), `plugins/catalogo_core/Services/CatalogoCurrencyFormatter.php` (new), `plugins/catalogo_core/Controller/VentasOpcionales.php` (modified), `plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php` (new), `plugins/catalogo_core/tests/TarifOpcionalesControllerMasterStateTest.php` (modified).

- [x] 1.1 **RED (new behavior test)** — Create `plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php` (namespace `Tests\CatalogoCore`; DB-free source/behavior contracts with anonymous subclasses, mirroring `TarifOpcionalesControllerMasterStateTest`). Author the trait behavior methods: `test_tarifa_selector_prefers_requested_then_default_then_first_active` and `test_no_active_tarifas_yields_null_selection` (OUM-01, AD-1); `test_filters_query_primary_and_search_alias_are_carried_in_url` (OUM-02, AD-6); `test_pagination_uses_count_filtered_true_total_and_preserves_filters` (OUM-02, AD-3); `test_state_cache_reads_effective_master_without_persistence` and `test_price_cache_maps_selected_tarifa_price` (OUM-03); `test_valid_toggle_persists_through_set_accessors` and `test_missing_or_invalid_csrf_blocks_toggle_and_persists_nothing` (OUM-04, AD-4); `test_new_opcional_normalizes_prices_and_persists` and `test_new_opcional_rejects_duplicate_or_bad_price` (OUM-05, AD-4); `test_delete_is_blocked_without_allow_delete` (OUM-06); `test_export_headers_and_selected_tarifa_price_and_filter_inheritance` (OUM-07, AD-11). Run `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php` → RED (trait/methods missing). ~320 lines.
- [x] 1.2 **RED (existing contract repoint)** — In `plugins/catalogo_core/tests/TarifOpcionalesControllerMasterStateTest.php` change `LIST_CONTROLLER` to `plugins/catalogo_core/Controller/VentasOpcionales.php` and `LIST_VIEW` to `plugins/catalogo_core/View/ventas_opcionales.html.twig`, then repoint the list-only methods `test_list_state_cache_resolves_effective_master_per_tarifa`, `test_list_state_cache_marks_master_active_without_price_rows`, `test_list_resolves_state_from_master_not_price_rows`, `test_list_declares_csrf_guarded_toggle_endpoints`, `test_list_toggle_redirect_preserves_active_filters`, `test_list_toggle_forms_carry_the_active_filters`, `test_views_reuse_the_toggle_button_group_macro` (list entry) and `test_mutating_view_forms_carry_a_csrf_field` (list entry); `test_controllers_load_the_master_model` iterates the new path. Keep every edit/precios method untouched. Run the file → RED (unified controller/view do not yet carry the absorbed contracts). ~40 changed lines.
- [x] 1.3 **GREEN (trait)** — Create `plugins/catalogo_core/extras/VentasOpcionalesListTrait.php` (global trait, `require_once` chain mirroring `extras/TarifarioOpcionalStateTrait.php`): props `$b_codfamilia`, `$b_codtarifa`, `$b_solo_activos`, `$b_url`, `$offset`, `$resultados`, `$total_resultados`, `$tarifa_seleccionada`, `$tarifas`, `$tarifa_defecto`, `$familia`, private `$precios_cache`/`$opcionales_state_cache`; methods exactly per design §"Interfaces / Contracts": `ensure_opcionales_tables()`, `init_opcionales_list()`, `ini_filters()`, `resolve_tarifa_seleccionada()`, `search_opcionales()`, seams `opcional_master_state()`/`opcional_precio_model()`, `load_opcionales_state_cache()`, `load_precios_cache()`, accessors, `guard_mutating_action()` (`POST && validateFormToken()`), `toggle_opcional_state()`, `redirect_to_list()`, `new_opcional()` (CSRF-guarded), `delete_opcional()`, `export_excel_opcionales()` (ported verbatim), and pagination `hasMoreResults()`/`getPaginationUrl()`/`buildListUrl()`/`getPaginationItems()`/`getListQueryParams()`. The trait MUST NOT reference `fbase_paginas`, `requireCsrf`, `getRequest`, `isHtmxRequest`, `show_precio` or `simbolo_divisa`. ~500 lines.
- [x] 1.4 **GREEN (service)** — Create `plugins/catalogo_core/Services/CatalogoCurrencyFormatter.php` (`namespace FSFramework\Plugins\catalogo_core\Services`, `final`, static `symbol(string $coddivisa): string` / `format(float $value, string $coddivisa): string`), backed by the catalogo_core `extras/fs_divisa_tools.php`, per AD-5; wire the trait accessors `show_precio_opcional($id)` and `simbolo_divisa_tarifa($coddivisa)`. ~70 lines.
- [x] 1.5 **GREEN (controller)** — In `plugins/catalogo_core/Controller/VentasOpcionales.php` add `use \VentasOpcionalesListTrait;` and `public const TOGGLE_ACTIONS = ['toggle_activa','toggle_en_catalogo','toggle_en_tarifa'];`, and rewrite `privateCore()` per design §"VentasOpcionales" order: resolve `allow_delete`; toggle action → `toggle_opcional_state()` (exit); `action=export_excel_opcionales` → `export_excel_opcionales()` (exit); POST `delete` → `delete_opcional()`; POST `codigo`+`nombre` → `new_opcional()`; then `init_opcionales_list()`, `load_opcionales_state_cache()`, `load_precios_cache()`. Keep `getPageData()` (`name=ventas_opcionales`, `showonmenu=true`) and the `privateCore(&$response, $user, $permissions)` signature frozen (AD-2, OUM-12). ~+150/−30 lines.
- [x] 1.6 **GREEN + regression gate** — Run the two focused files, then the full suite: `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php plugins/catalogo_core/tests/TarifOpcionalesControllerMasterStateTest.php` and `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml`. `VentasOpcionalesControllerTest`, `TarifOpcionalesControllerContractTest`, `TarifOpcionalPreciosControllerTest`, `TarifConfiguradorOpcionalesTest`, `TarifarioOpcionalStateTraitTest`, `OpcionalDomainModelOwnershipTest` must stay green. Record RED→GREEN evidence.

## Phase 2 — WU-2: unified list view on htmx 4 + Alpine CSP (OUM-08, OUM-12; AD-10)

Target files: `plugins/catalogo_core/View/ventas_opcionales.html.twig` (modified), `plugins/catalogo_core/tests/CatalogoOpcionalesHtmxContractTest.php` (new), `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` (modified).

- [x] 2.1 **RED (new htmx contract test)** — Create `plugins/catalogo_core/tests/CatalogoOpcionalesHtmxContractTest.php` (namespace `Tests\CatalogoCore`; source contracts reading real template files) with: `test_list_view_boots_htmx_and_alpine_macros` (assert `Macro/Htmx.html.twig` + `Macro/Alpine.html.twig` imports, `{{ htmx.boot({'allowScriptTags': false}) }}`, `{{ alpine.boot() }}`); `test_list_filters_use_hx_get_body_swap_and_url_push` (assert `hx-get`, `hx-target="body"`, `hx-select="body"`, `hx-swap`, `hx-push-url="true"`, `hx-boost="true"` on the `query`/`b_codfamilia`/`b_codtarifa`/`b_solo_activos` filters); `test_list_toggles_and_mutations_use_post_and_never_hx_delete` (assert `hx-post` / `htmx.ajax('POST'`, no `hx-delete`); `test_list_view_registers_alpine_data_with_nonce_and_init_guard` (assert `Alpine.data(`, `alpine:init`, `{{ csp_nonce_attr() }}`); `test_list_view_uses_colon_events_and_no_v2_names` (assert `htmx:after:swap`/`htmx:after:request`, absence of `htmx:afterSwap`/`htmx:afterRequest`); `test_list_view_has_no_bootbox_and_no_raw` (assert no `bootbox`, no `|raw`); `test_list_view_preserves_locked_names_csrf_and_toggle_macro` (assert `name="query"`, `b_codfamilia`/`b_codtarifa`/`b_solo_activos`/`offset`, `{{ csrf_field() }}`, `tarif.toggle_button_group(`). Run → RED. ~200 lines.
- [x] 2.2 **RED (existing list contract repoint)** — In `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` repoint `LIST_VIEW` to `plugins/catalogo_core/View/ventas_opcionales.html.twig` and update `test_list_filters_use_hx_get_with_url_push` + `test_list_and_precios_views_preserve_locked_names_csrf_and_toggles` to the unified view; keep the edit/precios methods (incl. `simbolo_divisa` and `hx-delete` bans) untouched. Run → RED. ~25 changed lines.
- [x] 2.3 **GREEN (view rewrite)** — Rewrite `plugins/catalogo_core/View/ventas_opcionales.html.twig` on htmx 4 + Alpine CSP per AD-10: `htmx.boot({'allowScriptTags': false})` + `alpine.boot()`; full-page filter swaps via `hx-get` + `hx-target="body"` + `hx-select="body"` + `hx-swap="outerHTML"` + `hx-push-url="true"` + `hx-boost="true"`; per-tarifa state toggles as `hx-post` forms with `{{ csrf_field() }}` + `tarif.toggle_button_group(...)`; nonce'd `Alpine.data()` components `opcionalConfirm` (delete → `htmx.ajax('POST', url, {values:{delete:id, _csrf_token}})`) + `opcionalNew` + `opcionalExport` behind `document.addEventListener('alpine:init')` with `[x-cloak]`; `x-text` never `innerHTML`; native `fsc.getPaginationItems()` links; no bootbox, no `|raw`. Keep `new`/`edit`/`precios` action links and the locked filter names. ~+430/−180 lines.
- [x] 2.4 **GREEN + regression gate** — Run `plugins/catalogo_core/tests/CatalogoOpcionalesHtmxContractTest.php` + `plugins/catalogo_core/tests/TarifOpcionalesHtmxContractTest.php` + `plugins/catalogo_core/tests/VentasOpcionalesControllerTest.php`, then the full plugin suite green.

## Phase 3 — WU-3: delete `tarif_opcionales` and repoint every link (OUM-09, OUM-11; AD-9)

Target files: `plugins/catalogo_core/controller/tarif_opcionales.php` (delete), `plugins/catalogo_core/View/tarif_opcionales.html.twig` (delete), `plugins/catalogo_core/Init.php` (modified), `plugins/catalogo_core/model/tarif_opcional.php` (modified), `plugins/catalogo_core/model/tarif_opcional_precio.php` (modified), `plugins/catalogo_core/View/tarif_opcional_edit.html.twig`, `plugins/catalogo_core/View/tarif_opcional.html.twig`, `plugins/catalogo_core/View/tarif_opcional_precios.html.twig` (modified), the 4 `plugins/tarifario/View/tarif_*.html.twig` links (modified), `plugins/catalogo_core/tests/TarifOpcionalesControllerContractTest.php`, `plugins/catalogo_core/tests/InitUpgradeTest.php`, `plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php` (modified).

- [x] 3.1 **RED** — In `plugins/catalogo_core/tests/TarifOpcionalesControllerContractTest.php` drop `tarif_opcionales` from `CONTROLLER_SLUGS` (3 survivors) and update `test_all_four_controllers_are_declared_in_catalogo_core`, `test_all_four_controllers_extend_fbase_controller`, `test_all_four_controllers_have_zero_tarif_controller_references`, `test_all_four_controllers_have_zero_tarifario_opcional_requires`, `test_all_four_controllers_use_the_opcional_state_trait` (rename to `test_all_surviving_controllers_*`, assert the 3 survivors stay on `fbase_controller` + `TarifarioOpcionalStateTrait`). Add `test_upgrade_retires_tarif_opcionales_page_idempotently` to `plugins/catalogo_core/tests/InitUpgradeTest.php` (mirror the `retireVentasFamiliasPage` assertion; two `Init::upgrade()` runs are a no-op after the first). Add `test_model_url_fallbacks_point_to_ventas_opcionales` + `test_deleted_page_has_no_controller_or_view` + `test_catalogo_and_tarifario_views_repoint_to_ventas_opcionales` to `plugins/catalogo_core/tests/CatalogoOpcionalesUnifiedControllerTest.php`. Run the three files → RED. ~90 lines.
- [x] 3.2 **GREEN (delete + repoint + retirement)** — Delete `plugins/catalogo_core/controller/tarif_opcionales.php` and `plugins/catalogo_core/View/tarif_opcionales.html.twig` (no redirect alias). Repoint the no-id fallbacks in `model/tarif_opcional.php` and `model/tarif_opcional_precio.php` to `index.php?page=ventas_opcionales` (keep the `id` branch on `tarif_opcional_edit`); repoint the back-links in `View/tarif_opcional_edit.html.twig`, `View/tarif_opcional.html.twig`, `View/tarif_opcional_precios.html.twig`; repoint the links in `plugins/tarifario/View/tarif_actualizar_precios.html.twig`, `tarif_articulo_precios.html.twig`, `tarif_articulos.html.twig`, `tarif_historial_precios.html.twig`. Add `private static function retireTarifOpcionalesPage(): void` to `plugins/catalogo_core/Init.php` (mirror `retireVentasFamiliasPage()`: `fs_page()->get('tarif_opcionales')`, `delete()` when found) and call it from `upgrade()` in its own try/catch. Do NOT touch `Services/CatalogLegacyTableMigration.php`, `Services/TarifOpcionalExtMigration.php` or `tests/DeadOpcionalTableReferenceTest.php` (those refer to the historical DB table, not the page). ~120 changed lines.
- [x] 3.3 **GREEN + audit gate** — Run the three focused files + full plugin suite green; then run the page-link audit `rg -n 'page=tarif_opcionales' plugins/catalogo_core plugins/tarifario --glob '!openspec/**' --glob '!vendor/**'` and confirm it matches no page link (only DB-table references if any, which are explicitly excluded). Clear the Twig cache.

## Phase 4 — WU-4: move the opcional Tarifas tab ownership to `catalogo_core` (OUM-10, OUM-11; AD-7, AD-8)

Target files (catalogo_core): `View/Hooks/ventas_opcional_tabs_after.html.twig`, `View/Hooks/ventas_opcional_tab_pane_after.html.twig`, `View/Hooks/partials/opcional_precios_rows.html.twig`, `View/Hooks/partials/opcional_tab_save_script.html.twig` (all new/moved), `controller/tarif_opcional_tab.php` (new), `Init.php` (modified), `tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` (new). Target files (tarifario): `Init.php` (modified), the 3 opcional `View/Hooks/*` templates (delete), `View/Hooks/partials/tab_save_script.html.twig` (modified), `controller/tarif_tab_precios.php` (modified), `tests/Integration/HookRegistrationTest.php` (modified).

- [x] 4.1 **RED (hook ownership test)** — Create `plugins/catalogo_core/tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` (namespace `Tests\CatalogoCore\Integration`; boot `catalogo_core\Init`, simulate `TwigLoaderEvent`/`TwigInitEvent`, mirror `tarifario/tests/Integration/HookRegistrationTest`) with: `test_catalogo_core_registers_opcional_hooks_once_across_rebuilds` (OUM-10; both `ventas_opcional_tabs_after` + `ventas_opcional_tab_pane_after` registered exactly once under the static guard); `test_catalogo_core_renders_opcional_tab_and_pane_without_tarifario` (OUM-10; `@catalogo_core` templates resolve and render); `test_tarif_opcional_tab_endpoint_contract_has_csrf_permission_and_no_bare_simbolo_divisa` (OUM-10; slug/class/`extends fbase_controller`/`TarifarioOpcionalStateTrait`/`requireCsrf()`/`puede_editar_opcional()`/`@catalogo_core/Hooks/partials/opcional_precios_rows.html.twig`; assert no bare `fsc.simbolo_divisa(` under the moved hooks tree). Run → RED. ~200 lines.
- [x] 4.2 **RED (tarifario hook registration test)** — In `plugins/tarifario/tests/Integration/HookRegistrationTest.php` set `FROZEN_HOOKS` to the article pair only (`ventas_articulo_tabs_after`, `ventas_articulo_tab_pane_after`), rename `test_init_registers_all_four_frozen_hooks` → `test_init_registers_the_two_frozen_article_hooks`, remove `test_opcional_hooks_render_guarded_by_is_new` (relocated to 4.1) and the opcional assertions of `test_catalogo_host_views_render_the_tarifas_tab_when_tarifario_is_active`; keep `test_articulo_hooks_render_tab_shell_and_pane`, `test_no_bare_simbolo_divisa_calls_in_hooks_tree` (article tree) and the idempotency test. Run with `ddev exec php vendor/bin/phpunit -c plugins/tarifario/phpunit.xml plugins/tarifario/tests/Integration/HookRegistrationTest.php` (or the root Plugins suite) → RED. ~−50/+15 lines.
- [x] 4.3 **GREEN (catalogo_core side)** — Add the four `View/Hooks/*` templates under `plugins/catalogo_core/` (tab header `page=tarif_opcional_tab&action=rows&ref={{ fsc.opcional.id }}` guarded by `{% if not fsc.is_new %}`; pane `#tab_tarifario_precios`; per-tarifa rows partial using `fsc.simbolo_divisa(tarifa.coddivisa)`; opcional save-script with idempotent window flag). Add `plugins/catalogo_core/controller/tarif_opcional_tab.php` (`class tarif_opcional_tab extends fbase_controller`, `use TarifarioOpcionalStateTrait;`, GET `action=rows&ref=` → `template=FALSE` fragment, POST `guardar_precio_tab` → `requireCsrf()` + `puede_editar_opcional()` admin/gestor JSON `{ok,message,html}`). In `plugins/catalogo_core/Init.php` add `OPCIONAL_HOOK_TEMPLATES`, static `$hooksRegistered`/`$viewExtensionsRegistered` guards, and a `registerViewExtensions()` step subscribing to `TwigLoaderEvent` (self-register the `@catalogo_core` namespace over `__DIR__.'/View'`) and `TwigInitEvent` (register the two opcional hooks) per AD-8. ~360 lines.
- [x] 4.4 **GREEN (tarifario side, additive-then-subtractive)** — In `plugins/tarifario/Init.php` remove the 2 opcional entries from `HOOK_TEMPLATES` (keep the 2 `ventas_articulo_*` entries). Delete `plugins/tarifario/View/Hooks/ventas_opcional_tabs_after.html.twig`, `ventas_opcional_tab_pane_after.html.twig`, `partials/opcional_precios_rows.html.twig`. In `plugins/tarifario/View/Hooks/partials/tab_save_script.html.twig` drop opcional fields/branch, keeping the article half. In `plugins/tarifario/controller/tarif_tab_precios.php` remove `rows_tipo()`, `render_opcional_rows_fragment()`, `opcional_precio_model()`, `guardar_precio_opcional()`, `puede_editar_opcional()` and the `tarif_opcional_precio` require; `render_rows_fragment()` always renders article rows (article output byte-identical). ~−240 lines.
- [x] 4.5 **GREEN + regression gate** — Run `plugins/catalogo_core/tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` + `plugins/catalogo_core/tests/Integration/CatalogoCoreHookMarkersTest.php` (four frozen host markers unchanged) + the tarifario `HookRegistrationTest`, then the full catalogo plugin suite and the root Plugins suite. Clear the Twig cache.

## Phase 5 — WU-5: htmx 4 + Alpine CSP for the moved opcional tab (OUM-08, OUM-10; AD-10)

Target files: `plugins/catalogo_core/View/Hooks/ventas_opcional_tabs_after.html.twig`, `View/Hooks/ventas_opcional_tab_pane_after.html.twig`, `View/Hooks/partials/opcional_precios_rows.html.twig`, `View/Hooks/partials/opcional_tab_save_script.html.twig`, `controller/tarif_opcional_tab.php`, `tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` (modified).

- [x] 5.1 **RED** — Extend `plugins/catalogo_core/tests/Integration/CatalogoOpcionalesHookOwnershipTest.php` with the moved-tab htmx/Alpine contract methods: `test_moved_tab_pane_boots_htmx_and_alpine` (assert `htmx.boot({'allowScriptTags': false})` + `alpine.boot()`), `test_moved_tab_mutations_use_hx_post_and_never_hx_delete` (assert `hx-post` / `htmx.ajax('POST'`, no `hx-delete`), `test_moved_tab_registers_alpine_data_with_nonce_and_init_guard` (`Alpine.data(`, `alpine:init`, `{{ csp_nonce_attr() }}`, `[x-cloak]`), `test_moved_tab_uses_colon_events_and_no_v2_names` (assert colon names, absence of v2 names). Run → RED. ~80 lines.
- [x] 5.2 **GREEN** — Migrate the moved rows/save fragment, pane and save script to htmx 4 + Alpine CSP (colon events only, nonce'd `Alpine.data()` behind `alpine:init`, `hx-post` + inherited `X-CSRF-TOKEN` header, `[x-cloak]`, `x-text` never `innerHTML`), preserving per-tarifa currency rendering via the `fbase_controller` `simbolo_divisa()`. ~+150/−120 lines.
- [x] 5.3 **GREEN** — Re-run the hook-ownership test + full plugin suite green.

## Phase 6 — WU-6: verification (plugin suite + PHPStan + audit) (OUM-12)

- [x] 6.1 **Plugin suite green** — `ddev exec php vendor/bin/phpunit -c plugins/catalogo_core/phpunit.xml` → `OK, but there were issues! Tests: 511, Assertions: 1756, Warnings: 25, Skipped: 1.` (exit 0). `plugins/tarifario/phpunit.xml` → `OK, but some tests were skipped! Tests: 191, Assertions: 705, Skipped: 3.` (exit 0). `HookRegistrationTest` via the root Plugins suite → `OK (7 tests, 27 assertions)`. Counts recorded in `verify-report.md`.
- [ ] 6.2 **Static analysis** — `ddev exec composer phpstan` → `[OK] No errors` (188/188, exit 0). **WARNING (recorded in `verify-report.md`):** `phpstan.neon` declares `paths: [src, tests]` only and does NOT scan `plugins/catalogo_core`, so the green run does not actually cover the new trait/service/controller/endpoint or the new/updated test files. Stated intent not satisfiable by the current config; left unchecked.
- [x] 6.3 **Grep/diff audit** — `grep -rn 'page=tarif_opcionales' plugins/catalogo_core plugins/tarifario --include='*.php' --include='*.twig' --include='*.js' --exclude-dir=vendor --exclude-dir=openspec` → exit 1, 0 matches; `bootbox`/`|raw`/`hx-delete`/v2 htmx names absent from the unified list and moved hooks; article surface `git diff --stat` empty; core `openspec/` has no `unificar-opcionales-en-ventas-opcionales` entry and no `opcionales-management` spec. All recorded in `verify-report.md`.
- [ ] 6.4 **Runtime smoke checklist (dev DB, record in `verify-report.md`)** — **OPEN / NOT EXECUTED:** no authenticated browser/DEV-DB session or seeded fixture was available in this verify pass. Compliance is proven by DB-free behavior tests + source contracts plus the static audits. Items (a)–(g) remain deferred and are recorded as an explicit OPEN warning in `verify-report.md`; execute once a session exists and record results in `archive-report.md`.
- [x] 6.5 **Deploy/rollback hygiene** — Twig cache cleared after the change (and after the WU-3 deletion); rollback is `git revert` of the WU commits in reverse (WU-5 → WU-1) plus the companion tarifario branch, per `proposal.md §Rollback Plan`. Procedural; honored by apply and documented in `verify-report.md`.

## Acceptance notes

- `ventas_opcionales` is the single canonical opcionales page on `PageController` and exposes the full absorbed surface: selector, filters, per-tarifa state/price, CSRF-guarded toggles, creation, delete, Excel export, native filtered pagination (OUM-01..OUM-07).
- `tarif_opcionales` (controller, view and list-specific contracts) is gone with no redirect alias; the `fs_page` row retires idempotently and every page link/model fallback points to `ventas_opcionales` (OUM-09).
- The opcional "Tarifas" tab renders from `catalogo_core` alone (hooks + `tarif_opcional_tab` endpoint) with an idempotency guard; `tarifario` keeps only the `ventas_articulo_*` hooks and the article path of `tarif_tab_precios` byte-identical (OUM-10, OUM-11).
- Both the unified list and the moved tab use htmx 4 + Alpine CSP: colon events only, nonce'd scripts, `[x-cloak]`, no bootbox, no `|raw`, no `hx-delete` (OUM-08, OUM-12).
- Excel export parity holds: exact header set, resolved familia/subfamilia, selected-tarifa price, active-filter inheritance (OUM-07).
- The surviving opcional controllers (`tarif_opcional_edit`, `tarif_opcional_precios`, `tarif_configurador_opcionales`) stay `catalogo_core`-owned on `fbase_controller` and untouched (OUM-11).
- Plugin suite is green at or above baseline **475 tests / 1517 assertions**; strict TDD (red → green) per WU.
- No model/XML/schema/config change and no Composer dependency → **no `vendor/` commit is required** for this SDD.
- All SDD artifacts remain under `plugins/catalogo_core/openspec/`; the core `openspec/` is never touched.

## Dependencies & follow-up notes

- **Sibling unarchived changes**: `plugins/catalogo_core/openspec/changes/absorber-opcionales-tarifa-en-catalogo-core/` (capabilities `opcionales-tarifa-management`, `catalogo-render-hooks`) and `plugins/catalogo_core/openspec/changes/opcionales-por-tarifa/` (capability `opcionales-tarifa-management`) are still in flight. This change adds only the new `opcionales-management` capability and **consumes** the master model/accessors; it introduces no delta for `opcionales-tarifa-management` or `catalogo-render-hooks`. Do not edit the siblings' models, hooks, lifecycle or specs, and do not cross-reference their tasks.
- **Canonical spec reconciliation at archive**: `plugins/catalogo_core/openspec/specs/opcionales-tarifa-selector/spec.md` still names the retired page (`tarif_opcionales`) at lines ~10, ~120, ~124 and ~141. That canonical spec MUST be reconciled at archive time to drop the `tarif_opcionales` references (the `tarif_opcional_precios` + surviving-controllers wording stays). This change's delta is confined to `specs/opcionales-management/spec.md`; do not rewrite the canonical `opcionales-tarifa-selector` spec during apply.
- **Cross-repo chain**: catalogo_core slices chain `stacked-to-main` on `catalogo_core@main`; WU-3/WU-4's tarifario edits are additive-then-subtractive and ship on a companion branch off `tarifario@master`, opened only after the catalogo_core slice that provides the replacement (hook templates + `tarif_opcional_tab`). Never land the tarifario removal before its catalogo_core counterpart, or the opcional tab goes dark.
- **WU ordering is a hard dependency**: WU-1a → WU-1b → WU-2 → WU-3 → WU-4 → WU-5 → WU-6; each stacked branch builds on the previous and each ends with the full plugin suite green before the next slice starts.
- **Open questions deferred to smoke** (design §"Open Questions"): orphan `fs_access` rows for the retired page (default: leave untouched, mirror `retireVentasFamiliasPage()`); `tarif_opcional_tab` visibility (`showonmenu=false`); `query` vs `search` alias precedence (`query` wins); ignored `tipo` param on `tarif_tab_precios` (article-only); empty-active-tarifa notice. If any fails at smoke time, apply the documented default and record it in `verify-report.md`.
